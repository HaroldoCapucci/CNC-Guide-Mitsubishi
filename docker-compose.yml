version: '3.8'

services:
  cnc-guide:
    build: .
    container_name: cnc-guide-3
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./backend/data:/app/backend/data   # Persistência do SQLite
    restart: unless-stopped
